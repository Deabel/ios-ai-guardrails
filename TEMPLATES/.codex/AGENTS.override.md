# Example override guidance

This file overrides the default `AGENTS.md` at the same directory level.
Use it only when you want a stronger local override.

Example rules:

- In this repo, always run targeted tests before proposing completion.
- Never add dependencies without explicit approval.

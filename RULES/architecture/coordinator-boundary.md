# Coordinator Boundary

Use a coordinator or router abstraction when navigation logic becomes non-trivial.

Prefer extracting navigation when:
- multiple screens are chained
- flows require branching
- navigation state pollutes ViewModel responsibilities

# Immutability

Prefer immutable state and value semantics.

Prefer:
- `let`
- immutable DTOs
- copying over shared mutable state when practical

Avoid mutation unless it is necessary for correctness or performance.
